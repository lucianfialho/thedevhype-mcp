import { PricingPage } from './pricing-page';

export const metadata = {
  title: 'Pricing — thedevhype',
  description: 'Subscribe to individual MCP tools or get the bundle and save 50%.',
};

export default function Page() {
  return <PricingPage />;
}
